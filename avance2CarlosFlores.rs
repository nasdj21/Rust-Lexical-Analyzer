let verdadero = true;
let falso = false;
let resultado_and = verdadero && falso;
let resultado_or = verdadero || falso;
let negacion = !verdadero;
/// Suma dos números enteros.
///
/// # Ejemplo
/// ```
/// let r = suma(2, 3);
/// assert_eq!(r, 5);
/// ```
let suma = |a, b| a + b;

let resultado = suma(3, 5);
let valor = obtener_numero();
let total = calcular(10, resultado);

//! Este módulo maneja operaciones matemáticas.
let suma = |a, b| a + b;
let multiplicar = |x, y| -> i32 { x * y };
let multiplicar = |x, y| -> i32 {
    x * y
};
let aplicar = |x| x * factor;
let foo = || 10;
println!("Hola Mundo!"); 
println!("Mi nombre es {}", nombre); 
println!("{} tiene {} años", nombre, edad); 
let lista = vec![1, 2, 3]; 
println!("Lista completa: {:?}", lista); 
let pi = 3.14159;
println!("Pi redondeado: {:.2}", pi); 
let resultado = suma(3, 5);
// Llamada simple
let resultado = suma(3, 5);

// Llamada sin argumentos
let valor = obtener_valor();

// Llamada con expresiones complejas
let total = calcular(a + b, c * 2);

// Llamada a closure guardado en variable
let suma = |a, b| a + b;
let resultado = suma(3, 5);

// Llamadas anidadas
let res = procesar(obtener_datos(10));

// Llamada con variables
let x = 10;
let y = 20;
let resultado = sumar(x, y);


/*
Hola
Este
es un comentario
*/

/*
Hola
Este
es un comentario
/*
Y este un anidado
*/
*/

///
